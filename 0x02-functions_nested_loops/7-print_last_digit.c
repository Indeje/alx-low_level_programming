#include "main.h"

/**
 * print_last_digit - Print last digit of number
 * @n: Number
 *
 * Return: last digit of number if successful, -1 if failure
 */

int print_last_digit(int n)
{
	int rem = -1;

	rem = n % 10;

	return (rem);
}
